// http://www.c-cpp.ru/content/max-min
#ifndef __MINMAX_H
#define __MINMAX_H

#include <stdlib.h>

#ifndef max
#define max(a,b)            (((a) > (b)) ? (a) : (b))
#endif

#ifndef min
#define min(a,b)            (((a) < (b)) ? (a) : (b))
#endif

#endif  /* __MINMAX_H */
